services:
  plan-it-batch:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: plan-it-batch
    ports:
      - "42200:8080"
    env_file:
      - .env
    networks:
      - plan-it-network

networks:
  plan-it-network:
    external: true