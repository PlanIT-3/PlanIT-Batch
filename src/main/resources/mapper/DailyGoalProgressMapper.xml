<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="woojooin.planitbatch.domain.mapper.DailyGoalProgressMapper">
    <insert id="insertDailyGoalProgressBatch" parameterType="java.util.List">
        INSERT INTO daily_goal_progress (goal_id, isa_progress, deposit_progress, progress_date, created_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.goalId}, #{item.isaProgress}, #{item.depositProgress}, #{item.progressDate}, NOW())
        </foreach>
    </insert>
</mapper>